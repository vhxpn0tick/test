// Single entrypoint for esbuild bundling
// Import config first so globals are available to other modules
import './paymenter-config.js';
import './script.js';

// If you later add more modules, import them here so they are included in the bundle.
